{
  "title": "SensorPush Local",
  "config": {
    "step": {
      "user": {
        "title": "Add SensorPush Local",
        "description": "Click Submit to begin monitoring your SensorPush sensors via Bluetooth proxies."
      }
    },
    "abort": {
      "single_instance_allowed": "SensorPush Local is already configured."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "SensorPush Local Options",
        "data": {
          "daily_audit_hour": "Daily audit hour (0–23)",
          "max_retries": "Max retry attempts after first failure (0–5)"
        },
        "data_description": {
          "daily_audit_hour": "Hour of the day to run the automatic battery audit, in local time.",
          "max_retries": "How many times to retry a failed GATT connection before giving up."
        }
      }
    }
  },
  "services": {
    "run_audit": {
      "name": "Run Manual Audit",
      "description": "Triggers an immediate Bluetooth GATT audit for all configured sensors."
    }
  }
}
